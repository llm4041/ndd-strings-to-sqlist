#pragma once
// ============================================================================
// qttestclass.h —— 插件导出函数的"宿主"声明文件
// ----------------------------------------------------------------------------
// NDD 主程序通过 QLibrary 加载本 DLL，查找两个 C 风格导出函数：
//   NDD_PROC_IDENTIFY(NDD_PROC_DATA*)
//   NDD_PROC_MAIN(QWidget*, QString, std::function<QsciScintilla*()>, ...)
// 此文件只是为了方便在 IDE / 样式工程中作为 C++ 文件存在，
// 真正被外部调用的入口在 qttestclass.cpp 中的 extern "C" 段。
// ============================================================================

#include "pluginGl.h"
